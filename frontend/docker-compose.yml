services:
  frontend:
    image: ghcr.io/criscros/deportedata/frontend:${FRONTEND_IMAGE_TAG:-latest}
    container_name: deportedata-frontend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${FRONTEND_PORT:-80}:80"
